server:
  port: 8081

spring:
  datasource:
    url: jdbc:postgresql://localhost:5888/postgres
    username: postgres
    password: mysecretpassword
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
    show-sql: true
