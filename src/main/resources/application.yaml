server:
  port: 8087
spring:
  datasource:
    password: password
    url: jdbc:mysql://localhost:5678/inventory
    username: user
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
    show-sql: true

student:
  balance: 5
  list:
    - Hello
    - Goodbye
    - Goodmorning